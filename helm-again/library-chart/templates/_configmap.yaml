{{- define "library-chart.configmap.tpl" -}}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name | printf "%s-%s" .Chart.Name }}
data: {}
{{- end -}}

{{- define "library-chart.configmap" -}}
{{ include "library-chart.until.merge" (append . "library-chart.configmap.tpl") -}}
{{- end -}}