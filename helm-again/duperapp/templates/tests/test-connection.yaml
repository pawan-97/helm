apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "duperapp.fullname" . }}-test-connection"
  labels:
    {{- include "duperapp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "duperapp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
