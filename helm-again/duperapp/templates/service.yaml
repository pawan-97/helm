apiVersion: v1
kind: Service
metadata:
  name: {{ include "duperapp.fullname" . }}
  labels:
    {{- include "duperapp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "duperapp.selectorLabels" . | nindent 4 }}
